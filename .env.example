###############################################
# Ejemplo de variables de entorno (editar)
###############################################

# Base de datos (SQLite por defecto)
# Para SQLite, Prisma resolverá file:./dev.db relativo a la carpeta prisma
DATABASE_URL="file:./dev.db"

# JWT para autenticación (requerido)
JWT_SECRET="cambia_esto_por_un_secreto_largo_y_unico"

# URL pública del sitio (usada en metadatos y llamadas internas)
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Email/SMTP (opcional; requerido para enviar correos)
# Si defines SMTP_HOST, el sistema intentará enviar correos
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="usuario"
SMTP_PASSWORD="password"
FROM_EMAIL="rifas@example.com"

# PayPal (opcional; requerido para pagos con PayPal)
# ENV puede ser: sandbox | live

# Logging (opcional): ERROR | WARN | INFO
LOG_LEVEL="WARN"

# Contacto admin (opcional)
ADMIN_EMAIL="admin@example.com"
ADMIN_PHONE="+580000000000"

# Notas:
# - Variables NEXTAUTH_* no son usadas por este proyecto.
# - Puedes generar un JWT_SECRET seguro con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
